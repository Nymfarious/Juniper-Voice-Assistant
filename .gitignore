# Dependencies
node_modules/
functions/node_modules/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
# The Windows one, on a Windows machine. It was COMMITTED here from the start and
# every sibling repo that bothered to list it had it right -- this was the gap.
desktop.ini

# Local env
.env
.env.local

# Build
dist/
build/
test-results/
playwright-report/

# Local-only media and credentials — never commit
# Holds personal voice recordings plus live secrets (a Twilio 2FA recovery
# code and a stray .env). Kept on disk deliberately; ignored so a stray
# `git add .` can't publish it.
Juniper Voice Assistant Audio/
*2FA*recovery*
*recovery_code*
_env*.txt
.vercel
.env*
